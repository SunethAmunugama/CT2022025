import javax.swing.*;

public class Q12 {
    static void main(String[] args) {
        JFrame windows = new JFrame();

        windows.setSize(300,200);
        windows.setTitle("My First Name");
        windows.setVisible(true);
        windows.setLocation(50,100);
    }
}